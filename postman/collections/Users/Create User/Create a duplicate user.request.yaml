$kind: http-request
name: Create a duplicate user
url: "{{URL_Base}}/usuarios"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: "{\r

    \  \"nome\": \"{{name}}\",\r

    \  \"email\": \"{{email}}\",\r

    \  \"password\": \"{{password}}\",\r

    \  \"administrador\": \"{{admin}}\"\r

    }"
auth:
  credentials:
    token: "{{auth}}"
  type: bearer
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code\", function () {\r

      \    pm.response.to.have.status(400);\r

      });\r

      \r

      pm.test(\"User duplicate\", function () {\r

      \    pm.expect(pm.response.json().message).to.eql(\"Este email já está
      sendo usado\");\r

      });"
    language: text/javascript
order: 4469144856906588
